import mypackage;

// Define the node
class Node {
    int data;
    Node next;

    // Node constractor to define the node variable
    public Node(int data) {
        this.data = data;
        this.next = null;
    }
}
